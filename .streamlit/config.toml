[server]
headless = true
address = "0.0.0.0"
port = 5000

[theme]
primaryColor = "#25D366"  # WhatsApp green for consistency
backgroundColor = "#0E1117"  # Dark background
secondaryBackgroundColor = "#262730"  # Darker shade for cards
textColor = "#FAFAFA"  # Light text for dark mode
font = "sans serif"